# version
version = 0.0.0

# paths
base.dir = .

bin.dir = ${base.dir}/bin
build.dir = ${base.dir}/build
src.dir = ${base.dir}/src
tests.dir = ${base.dir}/tests
vendor.dir = ${base.dir}/vendor

api.dir = ${build.dir}/api
coverage.dir = ${build.dir}/coverage
phpcb.dir = ${build.dir}/phpcb
pdepend.dir = ${build.dir}/pdepend
logs.dir = ${build.dir}/logs

# config files
apigen.conf = ${build.dir}/apigen.neon
phpcs.conf = ${build.dir}/phpcs.xml
phpmd.conf = ${build.dir}/phpmd.xml
phptest.conf = ${build.dir}/phptest.xml
phpunit.conf = ${build.dir}/phpunit.xml

# tools
apigen.bin = ${bin.dir}/apigen.php
doctrine.bin = ${bin.dir}/doctrine
pdepend.bin = ${bin.dir}/pdepend
phpcb.bin = ${bin.dir}/phpcb
phpcpd.bin = ${bin.dir}/phpcpd
phpcs.bin = ${bin.dir}/phpcs
phploc.bin = ${bin.dir}/phploc
phpmd.bin = ${bin.dir}/phpmd
phpunit.bin = ${bin.dir}/phpunit
